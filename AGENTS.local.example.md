# AGENTS.local.md — gitignored, never commit

Copy this file to `AGENTS.local.md` and fill in what applies.
Environment/developer-specific. Overrides AGENTS.md on conflict.

- Machine facts: OS quirks, ports in use, tool paths
- Where env files and credentials live — locations only, never contents
- Personal workflow: preferred test invocation, scratch dirs
- Temporary overrides: "CI deploy check is broken, skip until fixed"
