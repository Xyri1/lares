import { describe, expect, it } from 'vitest'

// ponytail: placeholder so `pnpm test` is green from commit one; replaced by real tests later.
describe('placeholder', () => {
  it('passes', () => {
    expect(1 + 1).toBe(2)
  })
})
